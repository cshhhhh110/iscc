# 行动日志

## 记录规则
- 这里记录当前赛题的总行动轨迹。
- 每完成一个阶段就追加一条新记录。
- 记录内容尽量只写：做了什么、结果是什么、当前进度百分比。

## 起点
- 2026-05-04 19:06:58
- 动作：建立总日志，确认当前赛题根目录结构，准备推进二进制漏洞检测任务。
- 结果：已完成目录骨架与通用 Conda 环境 `iscc-ml` 的搭建。
- 进度：20%

## 进展 1
- 2026-05-04 19:24:44
- 动作：补全 `源码/` 下的特征提取、训练、推理、工具脚本，并整理 `README.md`。
- 结果：形成可运行 baseline，使用 PE 头、字节统计、字符串/API 计数做两阶段建模；同时解压并平铺了 `binaries/` 样本目录。
- 进度：65%

## 进展 2
- 2026-05-04 19:37:00
- 动作：补充免激活运行脚本 `run_train.cmd`、`run_test.cmd`、`run_all.cmd`，并将 `README.md` 精简为目录和输出说明。
- 结果：运行入口从 PowerShell 激活切换为直接调用 `iscc-ml` 的 Python，可不依赖 `conda activate`。
- 进度：72%

## 进展 3
- 2026-05-04 19:40:00
- 动作：在训练和测试的特征提取循环中加入 `tqdm` 进度条。
- 结果：运行时可以直接看到样本级别进度，不再像卡住一样。
- 进度：75%

## 进展 4
- 2026-05-04 20:08:00
- 动作：将本题沉淀出的做题方式整理为通用技能 `data-security-competition`。
- 结果：技能已安装到 `C:\Users\30816\.codex\skills\data-security-competition`，并通过 skill 校验；适用于新的数据安全赛、机器学习竞赛和训练/测试/提交类项目。
- 进度：78%

## 进展 5
- 2026-05-04 20:18:00
- 动作：修复 CWE 多分类模型在长尾类别只有 1 条样本时报错的问题。
- 结果：CWE 模型显式关闭内部早停验证 `early_stopping=False`，并在最小类别样本数不足 2 时打印提醒；特征结构和提交格式不变。
- 进度：80%

## 行动 6
- 2026-05-05 00:20:13
- 动作：将 CWE 训练改成 warm_start 分批增树，并加入树级 tqdm 进度条。
- 进度：83%

## 行动 7
- 2026-05-06 00:00:00
- 动作：将当前升级版统一命名为 v1.1，并继续完善字节+表格多任务融合流程；只做编译和小范围 smoke 检查，不跑全量训练。
- 进度：86%

## 行动 8
- 2026-05-06 00:00:00
- 动作：完成 v1.1 版的语法检查与 2 条样本 smoke 测试，确认字节窗口、表格特征、神经模型 forward 和 batch 预测都能跑通。
- 进度：88%

## 行动 9
- 2026-05-06 12:10:00
- 动作：确认 `submission_v1.1.csv` 已生成，将当前 v1.1 源码、模型依赖与提交结果复制归档到 `版本记录/v1.1/`，并保留当前工作区继续作为 v1.1 工作版。
- 结果：提交文件 30974 行，列名为 `binary_id,label,cwe_id`，`label=0` 的 `cwe_id` 均为空，`label=1` 的 `cwe_id` 均非空。
- 进度：90%

## 行动 10
- 2026-05-06 16:04:36
- 动作：将当前工作区升级为 v1.2，改用版本化表格软投票集成、入口点优先字节窗口、byte embedding 残差 CNN，并提高 CWE 分支训练权重。
- 结果：`py_compile` 已通过；2 条样本 smoke 确认表格特征 `(2, 446)`、字节窗口 `(2, 8192)`、神经输出 `(2,)` 与 `(2, 86)` 正常；表格集成构造检查通过。
- 进度：94%

## 行动 11
- 2026-05-06 16:04:36
- 动作：将 v1.2 完整归档到 `版本记录/v1.2/`，并把活跃工作区切换到 v1.3，修正验证泄漏，让融合阶段只用 holdout 表格模型选权重。
- 结果：`py_compile` 与导入检查通过；v1.3 的源码、版本号和输出命名已更新，后续只需你重新跑 `train.py` / `test.py` 生成新的提交结果。
- 进度：97%

