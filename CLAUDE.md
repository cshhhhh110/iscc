# 赛题数据 — 解题工作区

> 一句话：先读目录和规则 → 最小可运行基线 → 按版本完整归档（源码/模型/结果）。

## 进入流程

1. **先读规则** — 打开 `总要求.md` 和 `ACTION_LOG.md`，了解版本规范、当前各项目状态和最近操作记录，不靠猜。
2. **最小可运行检查** — 不跑完整 `train.py`（除非用户明确要求），只做编译通过、数据加载、单折小规模 smoke 等可运行验证。
3. **按版本归档** — 每次有意义迭代，把当前 `源码/`、`模型/`、`提交结果/` 完整复制到 `版本记录/<version>/` 对应子目录。

## 关键约束

- `源码/` 只保留当前活跃版本代码，旧版进 `版本记录/<version>/源码/`。
- `模型/` 和 `提交结果/` 主产物文件名必须带版本号（如 `model_bundle_v1.2.joblib`、`submission_v1.2.csv`）。
- `smoke_*` 临时件验证后立即归档或清理，不堆在根目录。
- 每次更新都要写项目级 `ACTION_LOG.md`，并在总日志中追加记录（版本号、路径、产物、清理状态）。
- 不覆盖、不删除历史版本（含 `original`）。
- 根目录保持整洁：只留当前活跃版本产物。

## 子项目

| 项目 | 目录 |
|------|------|
| PowerShell 恶意脚本检测 | `powershell恶意脚本检测/` |
| 二进制文件漏洞检测 | `二进制文件漏洞检测/` |
| 系统日志异常检测挑战 | `系统日志异常检测挑战/` |
| 网络安全智能分类挑战 | `网络安全智能分类挑战/` |

每个子项目有独立的 `ACTION_LOG.md` 和 `README.md`，进入前先读。
